const Base_URL = "http://localhost:8000";
// const user = JSON.parse(localStorage.getItem("user"));

// export const AuthorizationHeader = { "Authorization": "Bearer " + user.token }

export default Base_URL;
